# alpha_ablation_us

My next step is going to be as following:
- Continue training in RL environment
- Apply graph algorithm so the machine can understand better the relationship between stocks, for example, if A drops, AAPL will have a greater chance of dropping too.
